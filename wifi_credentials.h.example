#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

// ============= CREDENCIAIS DE FALLBACK PARA DEBUG =============
// Copia este ficheiro para wifi_credentials.h e preenche com as tuas credenciais
// Estas credenciais sao usadas quando DEBUG_MODE=1 e os campos WiFi do portal estao vazios

#define FALLBACK_WIFI_SSID "YourNetwork1"
#define FALLBACK_WIFI_PASS "password1"

// Segunda rede opcional
#define FALLBACK_WIFI_SSID_2 "YourNetwork2"
#define FALLBACK_WIFI_PASS_2 "password2"

#endif // WIFI_CREDENTIALS_H
